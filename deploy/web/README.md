# Web Service Deploy Artifacts

- `cloudbuild.summary-web-service.yaml` (symlink) → `../../cloudbuild.summary-web-service.yaml`
- Dockerfile: `web-service/Dockerfile`
- 環境變數：請依 Cloud Run 設定與 Slack 短網址需求調整（見 Slack 部署 README）。

> 根目錄檔案仍為權威來源；此目錄提供一致路徑方便引用。如需在不支援 symlink 的環境，請直接使用根目錄檔案。***
