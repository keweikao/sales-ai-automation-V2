# Analysis Service Deploy Artifacts

- `cloudbuild.analysis.deploy.yaml` (symlink) → `../../cloudbuild.analysis.deploy.yaml`
- Dockerfile: `analysis-service/Dockerfile`
- 環境變數：目前部署流程保留 Cloud Run 既有設定（YAML 不覆寫）。調整前請先列出現有環境設定。

> 根目錄檔案仍為權威來源；此目錄提供一致路徑方便引用。如需在不支援 symlink 的環境，請直接使用根目錄檔案。***
