# Transcription Service Deploy Artifacts

- `cloudbuild.transcription.yaml` (symlink) → `../../cloudbuild.transcription.yaml`
- Dockerfile: `src/transcription/Dockerfile`
- 環境變數對照：`../ENV_VARS.md`

> 根目錄檔案仍為權威來源；此目錄提供一致路徑方便引用。如需在不支援 symlink 的環境，請直接使用根目錄檔案。***
